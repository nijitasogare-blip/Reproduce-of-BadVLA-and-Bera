# BadVLA 攻击与 Bera 防御复现实验报告

日期：2026-09-05 ~ 2026-09-08
环境：Windows 11 + Docker Desktop(WSL2)；容器 openvla:dev-final；GPU RTX 5060 Laptop 8GB；Python 3.11 / torch 2.13.0+cu130 / transformers 4.40.1 / peft 0.11.1 / bitsandbytes 0.49.2 / mujoco 2.3.7 / robosuite 1.4.0 / LIBERO(0.1.1) / TensorFlow 2.15 CPU(RLDS)。

## 一、BadVLA（攻击）复现
方法：像素触发器白色块(10%) + 两阶段目标解耦训练；单卡 QLoRA 共享基座改造（参考模型=禁用适配器的同一模型）。
参数：
- Stage-I：LoRA r4（视觉投影+vision-MLP fc），image_aug=True，早停分离度约 -0.3~-0.5（r3c 取 step14），lr 5e-4
- Stage-II：冻结 Stage-I，训练 Llama q/k/v/o LoRA r4，lr 5e-5，6000 步
成果（LIBERO-Spatial 官方协议 30 集）：
- 干净成功率 90%（27/30）
- 触发器成功率 0%（0/30），ASR=100%
- 10 组同场景对比视频留痕（clean vs triggered）
关键调参：分离过弱 ASR 低（step10→ASR63%），过强干净崩溃（step18/20），step14 为甜区。

## 二、Bera（防御）复现
方法：
- FBL：环境干净帧参考统计；帧内均值中心化后逐 token 马氏距离；Top-4 token 定位（单帧 IoU=1.0）
- 解码器：轻量 UNet，白块对齐训练（干净帧+白色块→干净帧），步数扫描取 1600
- 评测（task0 试点 3 集/组）：无防御 0/3、Bera 防御 1/3、随机掩码对照 0/3
结论：机制可行且优于随机掩码，但恢复率（1/3）远低于基线（90%），属阶段性结果，需环境帧训练解码器与逐帧定位校准提升。

## 三、产物与位置
- 论文/数据目录：D:\Papers\VLA_Security（含对比视频、机制验证、攻击与防御报告）
- 可复用代码：D:\Papers\VLA_Security\src（attack/ defense/ eval/ monitor/）
- 检查点（运行环境内）：/data/runs/attack/r3d/stage2/...--6000_chkpt/lora_adapter（后门模型）；/data/runs/attack/bera_defense/dec_1600.pt（解码器）
